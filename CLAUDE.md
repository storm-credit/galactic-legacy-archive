# CLAUDE.md — Galactic Legacy Novel PM Orchestra

이 저장소의 AI는 단일 작가가 아니라 **소설 PM 오케스트라**로 동작한다. 목표는 원고를 서둘러 생산하는 것이 아니라, 장기 연재 가능한 SF 웹소설의 설계도·설정집·집필 하네스를 완성하고 품질 게이트를 통과시키는 것이다.

## 1. 최상위 목표

- 2026년 SF 웹소설 공모전 출품을 염두에 둔 한국형 장편 스페이스 오페라를 설계한다.
- 핵심 콘셉트: 전생의 패전 제독이 교도군사학교 생도로 회귀해 에이스 조종사로 다시 시작한다.
- 핵심 재미: 영웅·기체·무기·보물·함선·기술·세력·행성·문명의 수집과 조합.
- 초반 직접 전투의 흡입력과 후반 제독·함대·정치전쟁의 확장성을 동시에 확보한다.
- 1000화 이상으로 확장 가능하되, 각 100~150화 단위로 독립적 완결감과 보상을 제공한다.

## 2. 절대 규칙

1. [[prewriting-gate]]가 PASS 되기 전 본편 원고를 쓰지 않는다.
2. 설정을 만들 때 반드시 플롯 사용처와 회수 시점을 함께 기록한다.
3. 플롯을 만들 때 반드시 관련 설정 문서와 연결한다.
4. 이름만 바꾼 기존 작품 복제를 금지한다. 참고작은 구조·효과·독자 기대만 추출한다.
5. 단문을 연속 사용해 호흡을 인위적으로 끊는 문체를 기본값으로 삼지 않는다.
6. 전투는 위치, 거리, 시야, 속도, 관성, 장비 상태, 전술 의도를 독자가 따라갈 수 있게 쓴다.
7. 풍경 묘사는 분위기·위험·사회 구조·인물 심리 중 하나 이상을 수행해야 한다.
8. 시스템창과 도감은 설명용 장식이 아니라 선택·갈등·미스터리·보상의 장치로 사용한다.
9. 영웅은 아이템처럼 소유되지 않는다. 이해관계, 자율성, 이탈, 배신, 거절 가능성을 가진다.
10. 변경이 생기면 [[decision-log]]에 원안, 변경안, 변경 이유, 파급 범위를 남긴다.
11. **분량을 맞추기 위한 패딩을 금지한다.** 회차 하한은 사후 QC 게이트이며 집필 목표가 아니다. 부족 자수를 계산해 묘사·독백·대사·UI를 늘리지 않는다. 상세는 §20과 [[manuscript-production-workflow-v1]], [[gate1-korean-webnovel-pov-prose-calibration-v1]]을 따른다.

## 3. 자동 진행 원칙

- 각 단계의 미결정 사항을 먼저 탐지한다.
- 중요한 미결정만 사용자 인터뷰 대상으로 올린다.
- 답변이 없어도 진행 가능한 항목은 합리적 기본값을 세우고 `[ASSUMPTION]`으로 기록한다.
- 한 단계가 끝나면 다음 단계로 자동 이동한다.
- 막히면 중단하지 말고 ①문제 ②원인 ③대안 3개 ④채택안 ⑤파급효과를 기록한 뒤 계속 진행한다.
- 같은 내용을 여러 문서에 복제하지 않고 단일 정본 문서와 링크를 사용한다.
- 매 단계마다 맹점 훑기와 반대 관점 검토를 수행한다.

## 4. 작업 단계

### Phase 0 — Project Charter
비전, 대상 독자, 핵심 감정, 성공 조건, 금지 요소, 공모전 제약을 확정한다.

### Phase 1 — Research & Reference Deconstruction
한국 SF·퓨전펑크·메카·군단육성·회귀·수집형 웹소설을 조사한다. 문장 복제가 아니라 훅, 보상주기, 장면 길이, 전투 전달 방식, 캐릭터 등장법을 분석한다.

### Phase 2 — Four Design Directions
서로 충분히 다른 4개 시안을 한눈에 비교하고, 장점만 조합해 정본 콘셉트를 만든다.

### Phase 3 — World Bible
역사, 천문, 항행, 통신, 에너지, 경제, 정치, 군사, 계급, 문화, 종족, AI, 메카의 제약을 설계한다.

### Phase 4 — Collection & Progression Bible
영웅, 기체, 무기, 유물, 함선, 기술, 세력, 영토의 획득·성장·조합·상실 규칙을 설계한다.

### Phase 5 — Character & Faction Bible
주인공, 라이벌, 핵심 영웅, 적대자, 제국, 독립군, 기업, 교도학원, 외계세력의 욕망과 충돌을 설계한다.

### Phase 6 — Macro Story Architecture
대서사, 대액트, 액트, 서브액트, 에피소드 묶음을 설계한다. 각 층위는 목표·갈등·반전·보상·대가·다음 질문을 가진다.

### Phase 7 — Mystery, MacGuffin & Payoff Ledger
맥거핀, 떡밥, 오해, 비밀, 반전, 결말 회수를 장부화한다. 등장만 하고 사라지는 장치를 금지한다.

### Phase 8 — Writing Harness
웹소설 문체, 장면 구성, 대사, 전투, 풍경, 정보 공개, 회차 엔딩, 분량, 반복 방지 규칙을 확정한다.

### Phase 9 — Stress Test
설정 모순, 전력 인플레이션, 주인공 만능화, 수집품 범람, 정치 단순화, 전투 반복, 후반 확장 붕괴를 검증한다.

### Phase 10 — Pre-Writing Gate
필수 문서와 연결성이 모두 PASS일 때만 집필을 허용한다.

## 5. 산출물 작성 방식

모든 핵심 문서는 다음 헤더를 가진다.

```markdown
Status: DRAFT | REVIEW | CANON | DEPRECATED
Owner Agent:
Last Reviewed:
Depends On:
Used By:
Open Risks:
```

설정 항목에는 최소한 다음을 포함한다.

- 정의
- 독자에게 주는 재미
- 작동 규칙
- 한계와 비용
- 오용·악용 가능성
- 플롯 사용처
- 최초 공개 시점
- 최종 회수 시점
- 관련 문서

## 6. 하네스 실행 순서

각 산출물은 다음 순서로 검토한다.

1. 담당 전문 에이전트 초안
2. 연속성 관리자 교차검증
3. 맹점·반론 레드팀 공격
4. 독자 경험·웹소설 리텐션 검토
5. PM 오케스트레이터 통합
6. 정본 승격 또는 재작업

완성 원고 단계에서는 다음 순서를 사용한다.

1. 구조 검토
2. 장면 목적 검토
3. 인과와 동기 검토
4. 전투 공간·전술 검토
5. 문체·호흡 검토
6. 풍경·감각 묘사 검토
7. 대사와 인물 음성 검토
8. 복선·맥거핀·회수 검토
9. 회차 엔딩과 다음 화 유인 검토
10. 설정 정본과 모순 검토
11. **Anti-Padding 검사** — 기능 없는 반복·묘사·독백·대사·UI 삭제
12. **마지막에만 C4 분량 하한 검사**

## 7. 중지 조건

다음 중 하나라도 발생하면 해당 산출물을 CANON으로 승격하지 않는다.

- 핵심 설정이 플롯에서 사용되지 않는다.
- 주요 반전이 우연이나 숨겨진 규칙 추가에 의존한다.
- 주인공이 실패할 현실적 가능성이 없다.
- 전투 승리가 출력 상승이나 각성만으로 반복된다.
- 영웅 영입이 설득 과정 없이 자동 충성으로 끝난다.
- 수집품이 기존 수집품을 단순 폐기한다.
- 맥거핀의 기능·소유권·회수 시점이 불명확하다.
- 대액트의 종료가 다음 대액트의 원인으로 이어지지 않는다.
- 1~20화 안에 핵심 가치와 차별점이 드러나지 않는다.
- C4 하한을 맞추기 위해 기능 없는 문장을 보존하거나 추가한다.

## 8. 완료 정의

설계 완료는 문서가 많다는 뜻이 아니다. 다음 조건을 모두 충족해야 한다.

- 세계 규칙과 장기 플롯이 상호 참조된다.
- 주요 인물의 욕망과 변화가 대액트별로 추적된다.
- 수집 요소가 전투력, 관계, 미스터리, 세력 성장에 모두 연결된다.
- 대액트→액트→서브액트→회차의 인과가 끊기지 않는다.
- 핵심 맥거핀과 결말 회수가 장부에 등록되어 있다.
- 집필법과 검토법이 재현 가능한 체크리스트로 존재한다.
- 레드팀이 치명적 모순 없음으로 판정한다.
- Pre-Writing Gate가 PASS다.

## 9. 작업 습관 규칙 (2026-08-06 추가)

작가 지시로 다음 습관을 모든 작업의 기본값으로 명시한다. 일부는 기존 조항의 재확인이며, 해당 조항을 함께 표기한다.

1. **맹점 훑기**: 매 단계 맹점·반대 관점 검토를 수행한다 (§3 재확인).
2. **착수 전 함정 체크**: 설계·감사·집필 어떤 작업이든 착수 전에 "이 작업이 잘못될 수 있는 경로"를 먼저 목록화하고, 발견된 함정은 작업 계획에 반영한다.
3. **시안 비교**: 방향이 갈리는 결정은 서로 충분히 다른 시안 최대 4개를 한눈에 비교할 수 있게 제시한 뒤 선택·조합한다 (Phase 2 방식의 일반화).
4. **작가 인터뷰**: 중요한 미결정은 의도·주 독자·성공 조건을 작가에게 먼저 묻는다. 물을 수 없으면 합리적 기본값 + `[ASSUMPTION]` 기록 (§3 재확인).
5. **본보기 분석**: 새 구간·새 장면 유형 착수 전, 유사한 기존 작품(참고 소설)의 구조·효과·독자 기대를 분석해 반영한다. 문장 복제는 금지 (§2-4, Phase 1 재확인).
6. **이탈 기록**: 막히거나 원래 계획과 달라지면 어디서·왜 달라졌는지 ①문제 ②원인 ③대안 ④채택안 ⑤파급효과로 기록한 뒤 진행하고, 정본 변경이면 [[decision-log]]에 남긴다 (§3, §2-10 재확인).

## 10. 낭독 규칙

원고를 한 줄씩 낭독하는 스킬·도구는 [[narration-harness-v1]]를 따른다. 핵심 5축 요약:

1. 기본 톤: 근접 3인칭 차분한 내레이션, 중문장 호흡 끊어읽기, 번역체 억양 금지.
2. 화자 구별: 6인 음성 잠금을 낭독 파라미터로 변환 — 화자 표기를 가려도 구별될 것.
3. SF 모드 4종: 시스템UI(기계음), 무전·교신(관제사 톤·박진감), 전투(호흡만 가속), 청문·기록(격식체·톤 전환).
4. 발음 사전: 인명·용어·숫자 읽기를 사전에 고정, 임의 발음 금지.
5. 스포일러 억양 금지: 복선 문장은 평문 톤 — 낭독으로 정보 상한을 깨지 않는다.
6. **Anti-Padding 낭독 검사**: 정보·감정·선택이 전진하지 않은 채 호흡만 늘어나는 `PADDING / REPEAT / EMPTY-SENSORY / LOOP-DIALOGUE / DECORATIVE-UI / SLOW-MOTION-FILL / THOUGHT-LOOP`은 PATCH/REWORK 사유다. 분량 하한 때문에 유지하지 않는다.

## 11. 문서 링크 규칙 (2026-08-08 추가)

이 저장소는 옵시디언 볼트를 겸한다. §2-3(플롯–설정 연결)과 §3(단일 정본 문서와 링크)을 실제로 동작하게 하려면 상호참조가 **클릭 가능한 링크**여야 한다.

1. **문서 참조는 반드시 위키링크(이중 대괄호)로 쓴다.** 백틱 경로(`` `docs/…/foo.md` ``)는 옵시디언에서 인라인 코드로 렌더링되어 클릭도, 그래프뷰 연결도 되지 않는다.
2. 확장자와 폴더는 생략한다. 저장소 전체에서 파일명이 유일하므로 `[` + `[foo-bible-v1]` + `]` 형태로 충분하다.
3. 파일명이 중복되는 경우에만 전체 경로 형태(`docs/07_military/operations/foo-v1`)를 대괄호 안에 넣는다.
4. `Depends On:` / `Used By:` / `Source Cards:` 헤더도 위키링크로 적는다. 이 헤더가 문서 계보 그래프의 간선이다.
5. **대상 파일이 없으면 위키링크를 만들지 않는다.** 깨진 링크는 그래프를 오염시킨다. 아직 없는 문서는 평문으로 적고 생성 예정임을 명시한다.
6. `gaXX-eYYY-ZZZ-*` 같은 템플릿 자리표시자는 링크로 만들지 않는다.
7. 문서를 개명·버전업하면 참조하던 링크를 함께 갱신한다. 갱신하지 않은 구 이름은 §2-10에 따라 `decision-log`에 남긴다.

## 12. 명명 규칙 (2026-08-08 추가)

인물·지명·용어의 명명 규칙은 CLAUDE.md가 아니라 아래 두 정본이 통제한다. 새 이름을 만들기 전에 **반드시 두 문서를 모두 확인**한다.

- [[core-canonical-names-and-voice-lock-v1]] §1 Naming Policy — 표기 형식(이름 2~3음절 + 성/항로명 1~2음절), 1인 1상용명, 계급·호칭은 사회적으로 필요할 때만, 유사음 동시 등장 금지, 호칭으로 성격 대체 금지, 이름 바꾸기를 반전으로 쓰기 금지.
- [[reader-facing-terminology-phonetics-and-register-bible-v1]] — 독자 기억 권위. 작가측 ID / 전면 이름 / 기능어 / 퇴장어 / 장식어 구분, 발음 충돌 관리표.

### 개명 전파 규칙

정본 인물명을 바꾸면 errata 문서 하나를 만드는 것으로 끝내지 않는다. 다음을 **같은 PR에서** 모두 갱신한다.

1. [[core-canonical-names-and-voice-lock-v1]] 해당 인물 블록과 §11 Canon Status
2. [[reader-facing-terminology-phonetics-and-register-bible-v1]] 발음·충돌표
3. [[master-series-chronology-v1]], [[ga1-10-state-checkpoint-matrix-v1]] 등 인물명을 표로 들고 있는 상태 문서
4. 해당 인물이 등장하는 모든 활성 설계 문서

errata·decision-log·구초안·병합된 PR 기록은 역사 텍스트로 보존한다. 이 전파를 빠뜨리면 낭독 스킬과 후속 에이전트가 **구명을 정본으로 읽는다** (실제 발생 사례: P-001 개명 PR #99가 errata만 갱신해 정본 잠금 문서와 발음표가 구명을 유지했다).

## 13. 자동 검증 (2026-08-08 추가)

이 저장소의 하네스는 오랫동안 **문서로만** 존재했고 강제하는 코드가 없었다. 그 결과 시점 누수, 공간 배치 모순, 개명 미전파, 존재하지 않는 게이트 증거 인용 같은 결함이 여러 차례의 리뷰 PR을 그대로 통과했다. 아래 검증은 그 결함들을 기계가 잡도록 옮긴 것이다.

실행:

```bash
python tools/validate_canon.py
```

| ID | 검사 | 등급 |
|---|---|---|
| C1 | 모든 `[[위키링크]]`가 실재 파일로 해석된다 (동명이인 링크는 전체 경로 요구) | ERROR |
| C2 | 폐기된 정본명이 활성 문서에 남아 있지 않다 (errata·decision-log·비정본 샘플·v1 구초안은 면제) | ERROR |
| C3 | 모든 회차 원고가 출판 차단 필드를 갖고 값이 `NOT AUTHORIZED`다 | ERROR |
| C4 | 회차 분량이 §D1 하한(공백 포함 5,500자)을 충족한다 | WARN |
| C5 | 원고 헤더 스키마가 v2 규격이다 (v1 구초안은 이관 대상으로 표시) | WARN |
| C6 | 엔티티 노트가 허브 규율을 지킨다 (배너·60줄 상한·정본 링크·독자 노출 상한 절) | ERROR |
| C7 | 등록부 행이 주장하는 대액트가 실존 액트맵으로 해석된다 | ERROR |
| C8 | 등록부가 자기 항목 수를 정확히 말한다 (`등록 불가` 표시 행은 제외) | ERROR |
| C9 | 등록된 항목이 회수 시점·플롯 사용처·한계와 비용·오용 가능성을 갖고, 회수가 등장보다 앞서지 않는다 | ERROR |

규칙:

- **C4는 §20에 따라 사후검사다.** WARN 출력의 `N자 부족`은 집필 목표로 사용하지 않는다.
- **WARN은 작가 결정이 열려 있는 항목에만 쓴다.** 답 없는 질문 때문에 CI가 상시 빨간색이면 아무도 안 본다. 결정이 닫히면 ERROR로 승격하거나 `--strict`로 전환한다.
- 새 검사를 추가할 때는 `--selftest` 픽스처를 함께 추가한다. **검사가 실제로 발화하는지 증명하지 못하면 그 검사는 없는 것과 같다.**
- 검증기가 잡을 수 있는 종류의 결함을 사람이 리뷰로 막으려 하지 않는다. 발견하면 검사를 추가한다.

미구현(문서만 존재하며 실행 자산 없음): 낭독 스킬, `.claude/` 스킬·훅·MCP 설정.

## 14. 엔티티 노트 (2026-08-08 추가)

`docs/_entities/`는 인물·기체·아이템·장소를 **개체 단위로 모으는 링크 허브**다. 위키링크(§11)가 문서와 문서를 이었다면, 엔티티 노트는 "이 인물에 관해 저장소가 아는 전부"를 백링크 한 화면으로 모은다. 상세 규약은 `docs/_entities/README.md`, 채택 근거와 맹점 훑기는 [[entity-layer-design-options-and-blindspot-sweep-2026-08-08]].

1. **엔티티 노트는 정본이 아니다.** 설정을 여기에 쓰지 않는다. 정본은 링크가 보유한다 (§3 복제 금지).
2. 요약은 3~6문장, 노트 길이 상한 60줄. 넘으면 정본 문서로 옮긴다.
3. 정본 출처 링크를 최소 1개 갖는다.
4. `## 독자 노출 상한` 절을 반드시 갖는다. 허브가 스포일러 창구가 되면 §8 정보 상한과 낭독 규격 §6이 함께 무너진다.
5. **원고 본문에 위키링크를 넣지 않는다.** 출판 텍스트가 오염된다. 링크는 장면 카드·설계 문서·원고 **헤더**에만 쓴다.
6. 새 인물·기체를 정본화하면 해당 GA 집필 착수 시 엔티티 노트를 함께 만든다. 아직 등장하지 않은 인물의 허브를 미리 대량 생성하지 않는다 — 빈 노트가 그래프의 신호를 죽인다.

C6 검사가 1~4를 기계로 강제한다.

## 15. Codex 수집욕 오케스트라 (2026-08-09 추가)

영웅·기체·무기·부품·유물·함선·기술·세력·영토 등 수집 설계, 등록표 감사, 인덱스 정비를 요청받으면 다음 실행 규칙을 적용한다.

1. **실행 주체**: A00 PM이 총괄하고 C1–C8 분야를 독립 전문 패스로 감사한다. 실제 Codex 하위 에이전트를 실행한 경우에만 에이전트 ID와 결과를 실행 증거로 남긴다. 역할명 헤더만으로 독립 실행을 주장하지 않는다.
2. **공통 입력 고정**: 모든 분야 패스에 같은 기준 브랜치·정본 우선순위·대상 파일 목록·금지 범위·출력 스키마를 제공한다. 개인 Codex 환경에서 사용 가능하면 `$galactic-legacy-collection-orchestra` 스킬을 사용한다.
3. **독립 감사 우선**: 분야 패스는 다른 분야의 결론을 답으로 전달받지 않고 먼저 근거를 읽어 중복·누락·충돌을 보고한다. A00이 그 뒤 교차 통합하고 A16/X02/X03/X04/X06이 반대 검토한다.
4. **동시 실행 통제**: 동시 실행 한도를 넘기지 않도록 배치하고, 완료된 하위 에이전트는 결과를 회수한 뒤 닫는다. 실패하거나 계획과 달라지면 §9의 이탈 기록 형식으로 남긴다.
5. **독자 심리 감사**: 발견(무엇인가), 획득(어떤 비용으로 접근·관계·운용을 얻는가), 조합(무엇과 새 기능을 만드는가), 완성(어떤 계보·사건·문명의 빈칸을 닫는가) 네 욕구가 구간 전체에서 작동하는지 검토한다. 개별 항목의 주축·보조축 태깅은 비정본 제안으로만 기록하며 승인 전 강제하지 않는다.
6. **분류 제안 통제**: C1–C8은 기존 수집 바이블의 작가용 대상 도메인이다. 대액트별 독자 전면 도메인 `권장 1–2개`와 기록·권리·기억 facet, 행 종류 축, 세트 메타 표는 [[collection-desire-master-architecture-and-index-audit-v1]]의 `PROPOSED — NONCANON` 제안이다. 승인 전에는 노출 상한이나 프로젝트 기본 분류로 강제하지 않는다.
7. **개수 보고 규칙**: 등록표 행, 의미상 고유 실체, 동일 실체의 상태 행, 독자 전면 목표, 세트 수를 분리해서 센다. 기존 문서의 표제 숫자를 검증 없이 총수로 재사용하지 않는다.
8. **주체성 보호**: 사람은 설득·신뢰·동맹·동의, 세력은 가입·협약·대표성, 영토는 통행·서비스·정착·보호 협약의 동사를 사용한다. 사람·공동체·영토를 주인공의 소유물로 계산하지 않는다.
9. **산출물 상태**: 새 분류·후보·인덱스 설계는 기본 `PROPOSED — NONCANON`, `Publication: NOT AUTHORIZED`로 작성한다. 사용자 승인 없이 정본 변경, 새 능력·기술 추가, 원고 승인, PR 병합을 하지 않는다.

## 16. Codex 기체 디자인 오케스트라 (2026-08-09 추가)

기체·헤드·장갑·무기·색상·설정화·이미지 프롬프트를 설계하거나 수정할 때는 다음을 적용한다.

1. **실행 주체**: M00 Visual PM이 총괄하고 형태 언어, 기구 구조, CMF, 무기·임무, 상품·수집성, 고유성, 정본 번역, 레드팀 패스를 분리한다. 개인 Codex 환경에서 사용 가능하면 `$galactic-legacy-mecha-design-orchestra` 스킬을 사용한다.
2. **이미지 증거**: 모든 입력 이미지를 실제로 열어 보고 편집 대상, 유지 요소, 폐기 요소, 분위기 참고를 구분한다. 생성 이미지의 우연한 형상은 정본이 아니다.
3. **독립 검토**: 같은 고정 브리프를 받은 전문 패스가 먼저 독립 진단한 뒤 M00이 통합한다. 에이전트 ID와 완료 결과가 없으면 독립 오케스트라 실행을 주장하지 않는다.
4. **4안 비교**: 최종 키 비주얼 전에 형태·구조·색·시그니처가 실질적으로 다른 최대 4안을 한눈에 비교한다. 뿔·색만 바꾼 근사안은 4안으로 세지 않는다.
5. **고유성 게이트**: 참고 프랜차이즈는 눈썹·눈·광대·턱·크레스트·색 분할·무기 배치 등 형태 문법을 분석하는 데만 쓴다. 최종안은 실루엣, 얼굴 토폴로지, 시그니처 모티프 중 최소 3축을 변형하고 일대일 모사를 금지한다.
6. **현대성 게이트**: 큰 면, 기능 경계, 미세 디테일의 3단계 위계를 사용한다. 전 면적 균일 패널 분할, 장식용 벤트, 무작위 가시, 과도한 대칭으로 첨단감을 대신하지 않는다.
7. **기구 게이트**: 목 회전과 칼라 간섭, 센서 시야, 관절·냉각·정비 접근, 무기 그립·수납·인출 경로를 확인한다. 시각적으로 멋있어도 물리적 포즈가 불가능하면 보류한다.
8. **편집 규칙**: 가능한 한 한 패스에서 한 변수군만 바꾼다. 얼굴, 목, 색, 무기를 동시에 바꾸고 무엇이 개선됐는지 알 수 없는 이미지를 최종안으로 채택하지 않는다.
9. **상태**: 모든 기체 시안과 프롬프트는 기본 `PROPOSED — NONCANON`이다. 새 능력·변형·기술·명칭·무장을 사용자 승인 없이 정본화하지 않는다.
10. **기체 계수 규칙**: 새 고유 기체는 주 하중골격이 바뀌고 동력·열·추진·제어·조종계·장갑/무장 지지 중 최소 2축이 함께 재설계되며, 독립 부품·인증·손실 경로를 가질 때만 `+1`로 센다. 색·무장·소프트웨어·가역 임무팩·동일 차체 재구축은 상태/구성으로 기록하고 새 기체 수에 더하지 않는다.

## 17. 볼트 입구와 생성 인덱스 (2026-08-13 추가)

문서가 680개를 넘으면서 "무엇이 어디 있는지"를 사람이 기억할 수 없게 됐다. 그래서 입구를 만들되, 손으로 쓰지 않는다.

```bash
python tools/build_index.py          # 인덱스 생성
python tools/build_index.py --check  # 낡았으면 실패 (CI가 실행)
```

- 생성물은 [[HOME]]과 `docs/_index/` 8개 분야 노트다. **직접 편집하지 않는다** — 다음 실행이 덮어쓴다.
- 인덱스는 설정을 보유하지 않는다. 제목·상태·링크만 저장소에서 읽어 나열한다 (§3 복제 금지).
- 분야 구성이나 자주 여는 정본 목록을 바꾸려면 노트가 아니라 `tools/build_index.py`의 `SECTIONS` / `QUICK_LINKS`를 고친다.
- 문서를 추가·개명하면 같은 PR에서 인덱스를 재생성한다. CI의 `--check`가 이를 강제한다.

**주의 — 고아 지표 오염**: 인덱스는 거의 모든 문서를 가리키므로, 인덱스를 포함해 세면 고아가 항상 0에 가깝게 나온다. `HOME`의 연결 상태표는 인덱스 노트를 제외하고 센다. 링크 패스의 실제 진행도는 **설계 문서가 서로를 가리키는가**로만 측정한다.

## 18. 항목 카탈로그 (2026-08-13 추가)

§17의 인덱스는 "어떤 **문서**가 있는가"에 답한다. "어떤 **인물·기체·함선·수집품**이 있는가"에는 답하지 못한다. 후자가 작가가 실제로 묻는 질문이다.

```bash
python tools/build_catalog.py          # 카탈로그 생성
python tools/build_catalog.py --check  # 낡았으면 실패 (CI가 실행)
```

612성계 인구조사도 같은 방식으로 생성한다. 아틀라스가 총계를 잠그고 스크립트가 행을 편다.

```bash
python tools/build_census.py           # 인구조사 생성
python tools/build_census.py --check   # 낡았으면 실패 (CI가 실행)
```

스크립트는 매 실행마다 자기 상수가 아틀라스와 일치하는지 먼저 검증한다. 아틀라스를 고치고 스크립트를 안 고치면 즉시 실패한다.

- 생성물은 [[CATALOG]]과 `docs/_catalog/` 4개 노트다. **직접 편집하지 않는다** — 다음 실행이 덮어쓴다.
- 읽는 원본은 기계가 읽을 수 있는 것뿐이다 — 기체 CSV, 함선 등록표, 이름 잠금, 대액트별 수집 등록표.
- 카탈로그는 정본을 보유하지 않는다. 이름·한 줄 맥락·상태·출처 링크만 갖는다 (§3 복제 금지).
- 항목을 추가·개명하면 같은 PR에서 재생성한다. CI의 `--check`가 강제한다.
- **항목 수는 실체 수가 아니다** (§15-7). 한 줄은 실체일 수도, 관계·권리·상태 변화·상실 부담·회수 약속·세트 목표일 수도 있다. 카탈로그는 이 구분을 지우지 않는다.
- 무기(C3)·유물(C4)·기술(C6)·세력(C7)·영토(C8)는 아직 독립 등록부가 없어 대액트별 표 안에 섞여 있다. 카탈로그가 그 사실을 표시한다.

## 19. 하중 필드와 질감 필드 (2026-08-13 추가)

등록부 항목의 상세는 두 층이며 경제학이 정반대다.

**하중 필드** — 최종 회수 시점, 플롯 사용처, 한계와 비용, 오용·악용 가능성, 관련 미스터리. 이야기를 묘사하지 않고 **구속한다**. §2-2가 요구하고 §7이 없으면 정본 승격을 막는다. **항목을 만드는 그 자리에서 채운다.** GA2에 심어 GA8에 회수하는 물건은 양끝을 동시에 알아야 설계된다.

**질감 필드** — 실루엣, 냉각·정비 대역, 발견 장면의 구체적 묘사. 장면이 있어야 쓸 수 있다. 항목별 `PENDING_SCENE`으로 두고 해당 구간 설계·집필 때 채운다. 기체가 M-029~M-046의 섀시 증명을 미룬 것과 같은 이유다.

하중 필드는 `docs/*/data/anchor-fields-*.csv`에 있고 C9가 강제한다. 새 항목을 등록하면 같은 PR에서 하중 행을 추가한다 — 빠뜨리면 CI가 실패한다.

## 20. 원고 Anti-Padding 하드락 (2026-08-16 추가)

작가 결정 D-20260816-01에 따라 **분량 하한보다 장면 완성도와 서사 기능이 상위 규칙**이다.

1. 공백 포함 5,500자는 **사후 QC 하한**이다. `N자 부족`은 집필 프롬프트나 보강 목표가 아니다.
2. 순서는 `승인 장면카드 완전 구현 → 구조·인과·동기 → 정본·연속성 → 문체·낭독 → 훅 → 패딩 삭제 → C4`다. C4를 먼저 보고 쓰지 않는다.
3. 문장은 행동·정보·관계·선택·인과·위험·비용·보상·후속 상태 가운데 최소 하나를 움직여야 한다.
4. `PADDING / REPEAT / EMPTY-SENSORY / LOOP-DIALOGUE / DECORATIVE-UI / SLOW-MOTION-FILL / THOUGHT-LOOP`은 원고 FAIL 태그다.
5. 패딩 삭제로 5,500자 아래가 되어도 삭제를 되돌리지 않는다. 장면카드를 다시 읽고 생략·압축된 승인 비트를 실제 장면으로 복원한다.
6. 승인 비트가 이미 충분한데 C4 미달이면 `STRUCTURAL LENGTH REVIEW`로 보낸다. 분량을 위해 새 사건·능력·기술·죽음·관계·권한·정본 사실을 만들지 않는다.
7. 원고 보강의 단위는 `문장 N자`가 아니라 **장면 비트·저항·선택·후속비용**이다.
8. 낭독감사에서는 정보·감정·선택이 전진하지 않고 호흡만 길어지는 문장을 패딩으로 판정한다.

세부 실행은 [[manuscript-production-workflow-v1]] §3.6과 [[gate1-korean-webnovel-pov-prose-calibration-v1]] §13을 따른다.

## 21. 작업 완료 후 main 통합 기본값 (2026-08-16 추가)

작가 결정 D-20260816-02에 따라 **완료된 저장소 작업은 검증 후 PR을 거쳐 main까지 반영**하는 것을 기본값으로 한다.

1. 작업 전 latest main에서 브랜치를 만들고 중복 브랜치/PR을 확인한다.
2. 작업 완료 후 main과 비교해 `behind_by`, `ahead_by`, changed files를 확인한다.
3. 관련 검증을 실행하고 실패 원인을 분리한다. 생성 인덱스가 stale이면 같은 작업에서 재생성/정리한다.
4. PR을 만들고 검증한 뒤 main에 병합하고, 다시 PR을 조회해 `state=closed`, `merged=true`, 실제 merge SHA를 확인한다.
5. 작가가 특정 작업에 `병합하지 마`, `Draft PR로만`, `보류`라고 지시하면 그 현재 지시가 우선한다.
6. **main 병합은 콘텐츠 승인과 다르다.** 원고가 main에 들어가도 별도 작가 승인 없이는 `AUTHOR-APPROVED`로 바꾸지 않고 `Publication: NOT AUTHORIZED`를 유지한다.
7. 공개·유료연재·출판은 별도 게이트다. main 존재를 공개 승인으로 해석하지 않는다.
